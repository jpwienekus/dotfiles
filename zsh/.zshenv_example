# This needs to by coppied to $HOME and remove example from the name
# That will allow all zsh config to be within .config dir
export ZDOTDIR="$HOME/.config/zsh"
export HISTFILE="$ZDOTDIR/.zhistory"
export HISTSIZE=10000
export SAVEHIST=10000
